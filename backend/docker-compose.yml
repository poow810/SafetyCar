services:
  backend:
    image: safetycar-dev-backend:latest
    container_name: safetycar-dev-backend
    networks:
      - backend-network
    # restart: unless-stopped
    expose:
      - "8080"

networks:
  backend-network:
    external: true